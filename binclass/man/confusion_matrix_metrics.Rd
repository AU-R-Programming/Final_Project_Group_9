% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codes.R
\name{confusion_matrix_metrics}
\alias{confusion_matrix_metrics}
\title{Confusion Matrix and Classification Metrics}
\usage{
confusion_matrix_metrics(y_true, y_pred, cutoff = 0.5)
}
\arguments{
\item{y_true}{A numeric vector of true binary response values (length: n).}

\item{y_pred}{A numeric vector of predicted probabilities (length: n).}

\item{cutoff}{A numeric value specifying the cutoff for classification. Default is 0.5.}
}
\value{
A list containing the confusion matrix and several performance metrics.
}
\description{
Generate a confusion matrix and compute performance metrics for binary classification.
}
\examples{
# Simulate data
set.seed(123)
y_true <- rbinom(50, size = 1, prob = 0.5)
y_pred <- runif(50)
metrics <- confusion_matrix_metrics(y_true, y_pred, cutoff = 0.5)
print(metrics)
}
