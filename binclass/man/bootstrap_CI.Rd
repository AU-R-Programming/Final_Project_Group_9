% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codes.R
\name{bootstrap_CI}
\alias{bootstrap_CI}
\title{Bootstrapped Confidence Intervals for Logistic Regression Coefficients}
\usage{
bootstrap_CI(X, y, n_bootstrap = 20, alpha = 0.05)
}
\arguments{
\item{X}{A numeric matrix of predictors (dimensions: n x p).}

\item{y}{A numeric vector of responses (length: n). Values must be 0 or 1.}

\item{n_bootstrap}{An integer specifying the number of bootstrap samples. Default is 20.}

\item{alpha}{A numeric value specifying the significance level. Default is 0.05.}
}
\value{
A data frame with lower and upper bounds of the confidence intervals.
}
\description{
Compute bootstrapped confidence intervals for the coefficients estimated
from logistic regression.
}
\examples{
# Simulate data
set.seed(123)
X <- cbind(1, matrix(rnorm(100), ncol = 2)) # Add intercept
y <- rbinom(50, size = 1, prob = 0.5)
ci <- bootstrap_CI(X, y, n_bootstrap = 100, alpha = 0.05)
print(ci)
}
